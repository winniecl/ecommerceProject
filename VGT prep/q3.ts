import React, { ReactNode } from "react";
const search = () => {
  const node = (word: string) => {
    return{
    value: word,
    children: React.ReactNode;
  }};
  const a = node("a");
  const b = node("b");
  const c = node("c");
  const d = node("d");
  const e = node("e");
  const f = node("f");
  const g = node("g");
  const h = node("h");
  const i = node("i");
  const j = node("j");
  const k = node("k");
  const l = node("l");
  const m = node("m");
  a.children;
};
